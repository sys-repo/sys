import { type t } from './common.ts';
import { DUMMY, v } from './u.ts';

export const impact: t.VideoMediaContent = {
  id: 'model.impact',
  title: 'Impact Model',
  video: v(DUMMY),
  timestamps: {},
};
