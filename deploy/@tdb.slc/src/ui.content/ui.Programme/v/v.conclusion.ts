import { type t } from './common.ts';
import { DUMMY, v } from './u.ts';

export const conculsion: t.VideoMediaContent = {
  id: 'conculsion',
  title: 'Conclusion',
  video: v(DUMMY),
  timestamps: {},
};
