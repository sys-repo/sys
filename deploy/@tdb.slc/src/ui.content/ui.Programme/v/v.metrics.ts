import { type t } from './common.ts';
import { DUMMY, v } from './u.ts';

export const metrics: t.VideoMediaContent = {
  id: 'metrics',
  title: 'Key Metrics',
  video: v(DUMMY),
  timestamps: {},
};
