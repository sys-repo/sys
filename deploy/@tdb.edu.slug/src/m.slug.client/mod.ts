/**
 * @module
 * Slug client ingress (HTTP + schema validation).
 *
 * Pure client-side domain loader.
 * No UI, no FS, no compilation concerns.
 */
/** HTTP-based slug tree client. */
export { SlugClient } from './m.Client.ts';
