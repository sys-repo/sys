import type { t } from './common.ts';

/** Template namespace for tree-driver scaffolds. */
export declare namespace TmplTreeDriver {
  /** Template module surface for bootstrapping new tree drivers. */
  export type Lib = {};
}
