import type { t } from './common.ts';

/**
 * Core helpers for bundled Agent Skill artifact access and materialization.
 */
export declare namespace SkillsCore {
  /** Core skill artifact helper surface. */
  export type Lib = {};
}
