/**
 * @module
 * Versioned registry of bundled Agent Skill artifacts for
 * lazy loading, materialization, and provenance-aware access.
 */
export { pkg } from './pkg.ts';

/** Type library (barrel file). */
export type * as t from './types.ts';
