name: ci

on:
  push:
    branches:
      - main
      - phil-work

jobs:
  deno:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write # The OIDC/ID token is used for authentication with JSR.
    environment: dev
    env:
      TEST_SAMPLE: ${{ vars.TEST_SAMPLE }}

    steps:
      - uses: actions/checkout@v3

      - name: Guard: no bidi/hidden Unicode chars
        run: .github/-scripts/Task.-prep.check-no-bidi.sh

      - name: 'Install ESM Runtime: Deno 2.x'
        uses: denoland/setup-deno@v1
        with:
          deno-version: v2.5.x

      - name: 'Install ES Modules from JSR: https://jsr.io/@sys'
        run: deno task help

      - name: Deno Info
        run: deno info && deno --version

      - name: System Info
        run: deno task help


      - name: publish module → "@sys/types"
        env: &publish-secrets
          DENO_SUBHOSTING_ACCESS_TOKEN: ${{ secrets.DENO_SUBHOSTING_ACCESS_TOKEN }}
          DENO_SUBHOSTING_DEPLOY_ORG_ID: ${{ vars.DENO_SUBHOSTING_DEPLOY_ORG_ID }}
          PRIVY_APP_ID: ${{ vars.PRIVY_APP_ID }}
          PRIVY_APP_SECRET: ${{ secrets.PRIVY_APP_SECRET }}
        run: |
          cd code/sys/types
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/std"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys/std
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/crdt-t"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys/crdt.t
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/color"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys/color
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/cli"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys/cli
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/crypto"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys/crypto
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/immutable"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys/immutable
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/fs"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys/fs
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/testing"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys/testing
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/process"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys/process
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/net"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys/net
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/event"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys/event
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/http"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys/http
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/yaml"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys/yaml
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/schema"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys/schema
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/jsr"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys/jsr
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/text"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys/text
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/tmpl-engine"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys/tmpl-engine
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/ui-state"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys.ui/ui-state
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/ui-css"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys.ui/ui-css
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/ui-dom"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys.ui/ui-dom
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/ui-react"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys.ui/ui-react
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/ui-react-devharness"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys.ui/ui-react-devharness
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/ui-react-components"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys.ui/ui-react-components
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/ui-factory"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys.ui/ui-factory
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/driver-deno"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys.driver/driver-deno
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/driver-vite"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys.driver/driver-vite
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/driver-automerge"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys.driver/driver-automerge
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/driver-farcaster"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys.driver/driver-farcaster
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/driver-process"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys.driver/driver-process
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/driver-mastra"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys.driver/driver-mastra
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/driver-monaco"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys.driver/driver-monaco
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/driver-slug"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys.driver/driver-slug
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/driver-prosemirror"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys.driver/driver-prosemirror
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/crdt"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys/crdt
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/tmpl"
        env:
          <<: *publish-secrets
        run: |
          cd code/-tmpl
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/tools"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys.tools
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@sys/dev"
        env:
          <<: *publish-secrets
        run: |
          cd code/sys.dev
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1

      - name: publish module → "@tdb/edu-slug"
        env:
          <<: *publish-secrets
        run: |
          cd deploy/@tdb.edu.slug
          deno task test --parallel
          for i in 1 2 3; do
              if deno publish --allow-dirty; then
                exit 0
              fi
              echo "publish failed (attempt $i), retrying in 5s..."
              sleep 5
          done
          echo "publish failed after 3 attempts"
          exit 1
