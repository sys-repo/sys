# @sys/cell info command plan

## Status

Final state: implemented and refined.

The plan is retained as the historical implementation record for the `@sys/cell info` surface and the adjacent formatter polish that grew out of it.

## Commit messages

Committed sequence:

- `aa22d0e99` — `feat(cell): add read-only info command`
- `d8d29c054` — `style(cell): refine info report layout`
- `ef365f449` — `fix(cell): make started service formatter width-aware`
- `1b2302cb0` — `fix(cell): make CLI formatters width-aware`
- `325bfce98` — `fix(cell): soften info version emphasis`
- `072ed56fc` — `fix(cell): tune info visual hierarchy`

If committing this plan record separately:

- `docs(agent): record cell info formatter plan`

## Goal

Add `@sys/cell info [dir]` as a read-only declaration report for a Cell folder.

`help` explains how to operate the tool. `info` reports what this Cell declares.

## Final contract

`info` reports descriptor facts only:

- Cell root
- descriptor path
- descriptor version
- declared services
- declared tasks

It must not:

- import endpoint modules
- read owner package configs
- scan `data/`
- probe ports or process state
- infer views or derived surfaces
- write, migrate, start, kill, or otherwise mutate

## Final output hierarchy

Minimal Cell:

```text
Cell
  root         .
  descriptor   -config/@sys.cell/cell.yaml
  version      1

Services
  none

Tasks
  none
```

Declared services/tasks:

```text
Cell
  root         ./cell.stripe
  descriptor   -config/@sys.cell/cell.yaml
  version      1

Services
  view
    use      Serve
    from     jsr:@sys/tools/serve
    config   -config/@sys.tools.serve/view.yaml
    modes    dev

Tasks
  sample:deploy
    steps    sample:deploy:prep → sample:publish
```

## Final formatter rules

- Section titles `Cell`, `Services`, `Tasks` are green.
- Top-level Cell fact labels are gray scan anchors.
- Nested Service/Task field labels are dim gray.
- Primary executable values are white.
- Descriptor version value is subtle gray so it remains factual without drawing primary attention.
- Paths/module refs are subtle/path-formatted.
- Empty sections render structurally as child sentinel:

```text
Tasks
  none
```

- One shared label-column width is computed across rows/sections.
- TTY path fitting uses canonical path formatting.
- Task arrows are generated by the formatter and cyan.
- Inline task chains stay one-line only when they fit.
- Overflowing task chains render as multiline structural chains.
- Ellipsis is cyan.

## Adjacent formatter outcomes

This work also established shared formatter primitives for nearby CLI surfaces:

- `FmtFields` owns section title, label tone, label width/padding, and indentation.
- `FmtFit` owns width-aware text/value/path fitting.
- `info`, service-board, and task-plan formatters reuse local CLI primitives instead of duplicating width and field behavior.
- `@sys/cli` owns pure URL display decomposition through `Cli.Fmt.Url.parts(...)` and `Cli.Fmt.Url.serviceParts(...)`.

## Implemented files of interest

- `code/sys/cell/src/m.cli/m.run/u.info.ts`
- `code/sys/cell/src/m.cli/m.run/u.run.ts`
- `code/sys/cell/src/m.cli/u.fmt/u.info.ts`
- `code/sys/cell/src/m.cli/u.fmt/u.fields.ts`
- `code/sys/cell/src/m.cli/u.fmt/u.fit.ts`
- `code/sys/cell/src/m.cli/u.fmt/u.services.ts`
- `code/sys/cell/src/m.cli/u.fmt/u.task.ts`
- `code/sys/cell/src/m.cli/u.help/u.info.ts`
- `code/sys/cell/src/m.help/yaml/info.yaml`
- `code/sys/cell/src/m.help/yaml/root.yaml`
- `code/sys/cell/README.md`

## Tests

Coverage includes:

- `info` reports minimal Cells without probing or mutation.
- `info` renders declared services/tasks without importing endpoints.
- Shared label width across sections.
- Stronger top-level Cell labels than nested detail labels.
- Subtle descriptor version value.
- TTY path fitting.
- Inline cyan task arrows.
- Multiline overflow task chains.
- Service-board narrow/tiny terminal safety.
- Task-plan narrow terminal safety.

Verified commands:

```sh
cd code/sys/cell && deno task test ./src/m.cli/-test/-info.test.ts
cd code/sys/cell && deno task test ./src/m.cli/-test/-task.test.ts ./src/m.cli/-test/-info.test.ts ./src/m.cli/-test/-fmt.services.test.ts
cd code/sys/cell && deno task check
cd code/sys/cli && deno task check
```

## Final STIER / DMIND assessment

The resulting CLI shape is intentionally quiet and scannable:

- `Cell` identity facts are discoverable without overemphasis.
- `Services` and `Tasks` preserve nested visual hierarchy.
- Empty state is explicit and structural.
- Width handling avoids terminal hard-wrap on normal terminals.
- The command remains observational and does not leak into status/probing semantics.

Relative to high-quality command lines, this is aligned with the best patterns: clear subject, low ornamentation, deterministic formatting, readable color semantics, and no hidden mutation.
