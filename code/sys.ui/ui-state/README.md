# @sys/ui-state

Framework-agnostic **UI state orchestration**.

This module contains **pure state machines, reducers, and command algebras** for UI-domain logic.
It has **no rendering**, **no DOM**, and **no framework bindings**.
