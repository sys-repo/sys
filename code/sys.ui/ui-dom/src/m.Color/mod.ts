/**
 * @module
 * Helper for working with color values.
 *
 * @example
 * ```ts
 * import { Color } from '@sys/ui-react
 *
 * const theme = Color.theme('Dark');
 * const myColor = Color.alpha(theme.fg, 0.3);
 * ```
 */
export { COLORS, Color } from './m.Color.ts';
