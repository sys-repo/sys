/**
 * @module
 * Mocking helpers for working with the DOM in unit-tests on the server.
 *
 * @example
 * ```ts
 * import { Mock } from '@sys/ui-dom/mock';
 * Mock.polyfill();
 * ```
 */
export { Mock } from './m.Mock.ts';
