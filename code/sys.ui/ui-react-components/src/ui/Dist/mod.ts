/**
 * @module
 * UI components for discovering and selecting entries
 * from a `dist.json` distribution index.
 */
import type { t } from './common.ts';
import { Dist as UI } from './ui.tsx';

export const Dist: t.DistLib = {
  UI,
};
