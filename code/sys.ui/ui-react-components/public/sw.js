/**
 * 🐷
 * Dev: Stub (Service Worker)
 * - Build to see prod service working operating.
 * - ref: `/src/worker.service/mod.ts`
 */

console.info('🐷 [dev-sw] dummy service worker in Vite (dev mode).');
