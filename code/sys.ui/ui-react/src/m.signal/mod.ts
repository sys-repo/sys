/**
 * @module
 * Signal (React) — reactive Signals via "@preact/signals-react".
 *
 * Extends the core Signal surface from "@sys/std/signal" with React hooks.
 */
export { Signal } from './m.Signal.ts';
