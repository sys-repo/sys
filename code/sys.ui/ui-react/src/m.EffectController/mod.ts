/**
 * @module
 * EffectController (React) — React adapter for the minimal orchestration/controller primitive.
 *
 * This module re-exports the core `EffectController` from `@sys/std/effect`,
 * establishing a stable React-facing import surface.
 *
 * The underlying controller logic remains framework-agnostic.
 */
export { EffectController } from './m.EffectController.ts';
