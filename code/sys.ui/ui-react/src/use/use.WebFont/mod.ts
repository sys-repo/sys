/**
 * @module useWebFont
 * Minimal entry for the WebFont utilities — provides `WebFont.inject`
 * for safely inserting `@font-face` definitions into the document head.
 *
 * See upstream pure (non-react) implementation: @sys/ui-css
 */
export { WebFont } from './m.WebFont.ts';
export { useWebFont } from './use.WebFont.ts';
