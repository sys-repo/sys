/**
 * @module
 * Catalog Schemas
 *
 * Aggregate of all per-view schemas in this catalog.
 * Keeps `mod.ts` clean and gives consumers a single import point.
 */
export { HelloSchema } from './ui/Hello/schema.ts';
