/**
 * @module
 * React adapter for @sys/ui-factory.
 * - `ReactHostAdapter`: core HostAdapter implemented with React elements.
 * - `ReactPlanView`: lightweight façade to render a resolved plan directly.
 */
export { ReactHostAdapter } from './m.HostAdapter.ts';
export { ReactPlanView } from './ui.PlanView.tsx';
