/**
 * @module
 * Reusable System UI composition over domain contracts.
 *
 * The root exports package identity only; concrete UI lives behind explicit ESM leaves.
 */
export { pkg } from './pkg.ts';

/** Type library. */
export type * as t from './types.ts';
