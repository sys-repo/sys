/**
 * @module
 * Canonical app splash primitive.
 */
import type { t } from './common.ts';
import { Splash as UI } from './ui.tsx';

/** App splash renderer composition surface. */
export const Splash: t.Splash.Lib = { UI };
