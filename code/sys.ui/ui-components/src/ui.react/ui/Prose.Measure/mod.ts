/**
 * @module
 * Reading geometry for prose:
 * measure, margins, gutters, and responsive constraints.
 */
import type { t } from './common.ts';
import { Measure as UI } from './ui.tsx';

export const ProseMeasure: t.ProseMeasure.Lib = { UI };
