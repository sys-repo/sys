/**
 * @module
 * Tabs primitive for composing discrete panel views.
 * */
import type { t } from './common.ts';
import { Tabs as UI } from './ui.tsx';

/** Tabs renderer composition surface. */
export const Tabs: t.Tabs.Lib = { UI };
