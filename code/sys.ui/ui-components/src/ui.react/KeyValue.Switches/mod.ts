/**
 * @module
 * KeyValue-shaped switches for labeled boolean controls.
 */
import type { t } from './common.ts';
import { Switches as UI } from './ui.tsx';

export const Switches: t.KeyValueSwitches.Lib = { UI };
