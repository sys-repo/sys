import { css } from './common.ts';

export const SpecStyles = {
  title: css({
    fontSize: 11,
    fontWeight: 600,
    opacity: 0.6,
    textTransform: 'uppercase',
    letterSpacing: 0.6,
  }),
} as const;
