/**
 * @external
 */
export * as A from '@automerge/automerge/next';

/**
 * @system
 */
export { Arr, Dispose, Err, History, Is, Num, Obj, Pkg, R, rx, slug, Str, Time } from '@sys/std';
export { Signal } from '@sys/ui-react';
