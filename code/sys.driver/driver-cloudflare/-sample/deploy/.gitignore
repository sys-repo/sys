# Generated by deno task build; the build record is required by push and serve.
/dist/
/dist.private/
/dist.public/
/dist.pins.json
# Legacy generated record; invalidated by the next build, never a read fallback.
/dist.selection.json
