# Distill: ranked actions after TMIND + STIER

## Ranked conclusions

### 1. Instrument first
This is the immediate next move.
Without phase timing, the current strongest claims remain plausible but unproven.

## 2. Use Vite-native easy levers next
Try, in order:
- `dev.warmup`
- minimal `optimizeDeps.include`

These are low-risk and upstream-aligned.

## 3. Narrow npm prewarm only if the measurements justify it
Its broadness is real, but we should not overfit to it before data.

## 4. Prepare for a deeper transport lane
If instrumentation shows transport subprocess resolution/transform dominating, the next serious lane should be:
- transform strategy redesign first,
- then broader resolution/cache improvements.

## 5. Keep call-site graph hygiene as a separate truth lane
`slc-data` likely has startup-path weight that should not be mixed into driver-only optimization claims.

---

## Ranked intervention map

### Now
- add instrumentation
- add narrow cold/warm perf probe
- measure at least one real outside-in call-site world

### Next
- add `dev.warmup`
- add minimal `optimizeDeps.include`
- remeasure

### Then if needed
- inspect/narrow npm prewarm
- design transport transform strategy replacement

### Later
- persistent transport cache
- persistent resolved metadata cache
- package-surface/browser-ready hot-package review

---

## Stop conditions
Stop the first packet if any of these truths become clear:
- Vite-native levers already produce a meaningful win
- transport is overwhelmingly dominant and deeper work is justified
- call-site app graph breadth is the main remaining perceived cost

## Final STIER synthesis
The current architectural rewrite created the right platform for real optimization.
The next move should stay narrow and evidentiary.
The highest-probability deep win remains transport, but the most disciplined immediate move remains measurement-first with one modest Vite-native tuning packet.
