# Distill: ranked actions after TMIND + STIER

## Ranked conclusions

### 1. Instrument first
Done.
The first local instrumentation run produced usable truth.

### 2. Hit `transport.resolveDeno` hardest next
This is now the strongest measured driver-owned bottleneck.

Current local signal includes:
- `transport.resolveDeno count=161`
- `transport.resolveDeno total=65142` cumulative resolve time
- repeated resolution of equivalent `@std/*` targets
- inconsistent-looking specifier variants (`https://...` and `https:/...`)

The next serious lane should focus on:
- repeated resolution reuse
- cache-key normalization
- duplicate equivalent specifier collapse

## 3. Narrow npm prewarm next as a shallow follow-up
This is now justified by measurement, not only suspicion.

Current local signal includes:
- `config.npmPrewarm specifiers=82`
- `config.npmPrewarm elapsed=1634`

## 4. Keep Vite-native levers available, but not first
Try later, after the first resolution/prewarm follow-up:
- `dev.warmup`
- minimal `optimizeDeps.include`

## 5. Keep transform strategy redesign queued, but not first in the current local proof
CLI transform is real, but in the first local run it was materially smaller than repeated resolution work.

## 6. Keep call-site graph hygiene as a separate truth lane
`slc-data` likely has startup-path weight that should not be mixed into driver-only optimization claims.

---

## Ranked intervention map

### Now
- inspect repeated `resolveDenoWith(...)` work
- inspect cache-key normalization / duplicate equivalent specifiers
- add narrow cold/warm perf probe if needed for easier comparison

### Next
- narrow `sys:npm-prewarm`
- remeasure

### Then if needed
- add `dev.warmup`
- add minimal `optimizeDeps.include`
- reconsider transform strategy redesign

### Later
- persistent transport cache
- persistent resolved metadata cache
- package-surface/browser-ready hot-package review

---

## Stop conditions
Stop the first packet if any of these truths become clear:
- Vite-native levers already produce a meaningful win
- transport is overwhelmingly dominant and deeper work is justified
- call-site app graph breadth is the main remaining perceived cost

## Final STIER synthesis
The current architectural rewrite created the right platform for real optimization.
The first measured local proof now points most strongly at repeated subprocess Deno resolution, with broad npm prewarm as the clearest shallow startup tax.
The next move should stay narrow and evidentiary:
- hit `transport.resolveDeno` reuse/normalization first,
- narrow `sys:npm-prewarm` second,
- and defer broader Vite-native tuning or transform redesign until after remeasurement.
