/**
 * Sample used to pass "deno task" commands to the entry point.
 *
 * @example
 * Invoked via deno run command, eg:
 * ```bash
 *  deno run jsr:@sys/http/server/start --port=1234
 * ```
 */
import '../src/-entry/-main.ts';
