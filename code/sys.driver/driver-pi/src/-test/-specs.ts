/**
 * @module
 * DevHarness visual specs.
 */
import type { t } from './common.ts';
export const ns = 'driver.agent';

/**
 * Specs:
 */
export const Specs = {
  // [`${ns}: name`]: () => import('../ui/component/-spec/-SPEC.tsx'),
} as t.SpecImports;
