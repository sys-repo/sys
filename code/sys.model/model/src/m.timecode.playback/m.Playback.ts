import { type t } from './common.ts';
import { Manifest } from './m.Playback.Manifest.ts';

/** Timecode playback manifest schema helpers. */
export const PlaybackSchema: t.TimecodePlaybackSchemaLib = {
  Manifest,
};
