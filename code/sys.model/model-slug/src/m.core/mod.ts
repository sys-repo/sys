/**
 * @module
 * Pure core helpers for `@sys/model-slug/core`.
 *
 * Scope:
 * - Slug-domain transforms with no filesystem dependency
 * - No client/network loading concerns
 * - No schema ownership; uses schema-defined types
 */
export { SlugTree } from './m.SlugTree/mod.ts';
