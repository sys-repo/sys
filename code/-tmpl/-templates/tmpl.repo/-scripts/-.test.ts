import { type t, describe, it, expect, expectTypeOf, Is, Obj, Str } from '../code/common/-test.ts';

describe('Repo: -scripts', () => {
  it('baseline: 🐷', async () => {
    expect(123).to.eql(123);
  });
});
