/**
 * Templater CLI types.
 */
export declare namespace TmplCli {
  /** Types for the `dsl` command. */
  export namespace Dsl {
    /** Supported DSL chapter output formats. */
    export type Format = 'human' | 'skill';
  }
}
