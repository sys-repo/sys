# Prompt: initialize a Cell

Use @sys/cell to initialize this folder as a new Cell.

Start by reading the public @sys/cell CLI help, then run the minimal init. Keep the result additive
and owner-correct: Cell owns only data/, view/, and -config/@sys.cell/cell.yaml. Preserve any
Pi-owned config.
