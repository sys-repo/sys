# Deploy Cell data

This sample intentionally carries no app-specific data.

Its purpose is to prove a vanilla Cell can bind a pulled view artifact and stage it for deployment without adding an application build or runtime service.
