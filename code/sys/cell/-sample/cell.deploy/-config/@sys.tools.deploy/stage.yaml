# Provider-neutral local staging endpoint for the Cell deploy sample.

source:
  dir: .

staging:
  dir: ./.tmp/staging

mappings:
  - mode: copy
    dir:
      source: view/.pulled/ui.components
      staging: ui.components
