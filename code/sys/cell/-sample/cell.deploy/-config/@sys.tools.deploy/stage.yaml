# Stage the pulled baseline view artifact.

source:
  dir: .

staging:
  dir: ./.tmp/deploy/stage
  clear: true

mappings:
  - mode: copy
    dir:
      source: view/.pulled/ui.components
      staging: .
