# Deploy Cell view

This folder is the target for the baseline UI components view artifact.

The view is pulled with `@sys/tools/pull` from:

```text
https://fs.db.team/ui.components/dist.json
```

into:

```text
./.pulled/ui.components/
```
