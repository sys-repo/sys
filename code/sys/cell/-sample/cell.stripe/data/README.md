# Stripe Cell data

This folder carries the sample Cell's DSL/stored meaning.

The first sample is intentionally small: payment-facing meaning and runtime/view binding pressure live here as ordinary files, while the Stripe view is a managed artifact under `../view/`.
