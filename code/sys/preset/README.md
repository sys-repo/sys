# @sys/preset
Curated dependency preset aliases for `@sys` projects that group stable import mappings into
named surfaces, reducing import-map churn while keeping composition explicit.

### Example
```ts
import { pkg } from 'jsr:@sys/preset';
```
