/**
 * @module
 * Tools for formatting `npm:` import specifiers.
 */
import type { NpmImportLib } from './t.ts';
import { specifier } from './u.specifier.ts';

/** Import specifier helpers. */
export const Import: NpmImportLib = { specifier };
