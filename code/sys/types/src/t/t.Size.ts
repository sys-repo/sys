type Width = number;
type Height = number;

/**
 * Represents the basic size of an element.
 */
export type Size = { width: number; height: number };

/** A size (x/y) represented as a tuple. */
export type SizeTuple = [Width, Height];
