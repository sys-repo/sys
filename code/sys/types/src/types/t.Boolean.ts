/**
 * Type representing any value that is considered "falsy" in JavaScript.
 * Falsy values are those that, when evaluated in a boolean context, are treated as `false`.
 */
export type Falsy = false | 0 | '' | null | undefined;
