import { type t, describe, it, expect } from '../-test.ts';
import { GlobIgnore } from './mod.ts';

describe('Glob.Ignore', () => {
  it('create', async () => {
    /**
     * TODO 🐷
     */
  });
});
