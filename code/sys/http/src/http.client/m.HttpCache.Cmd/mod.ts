/**
 * @module
 * HTTP cache command surface.
 *
 * Exposes the typed `CacheCmd` boundary for cache-control request/response
 * commands (for example worker/client command channels).
 */
export { CacheCmd } from './m.Cmd.ts';
