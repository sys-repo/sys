/**
 * @module
 * Tools for working with the standard system `{pkg}` metadata and
 * its compiled distribution packages (`dist.json` metadata + hash manifests).
 */
export { Dist } from './m/m.Dist.ts';
export { Pkg } from './m/m.Pkg.ts';
