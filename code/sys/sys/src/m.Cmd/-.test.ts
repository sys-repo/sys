import { type t, describe, it, expect } from '../-test.ts';
import { Cmd } from './mod.ts';

describe('Cmd', () => {
  describe('Cmd.main', () => {
    it('sample', () => {
      Cmd.main([]);
    });
  });
});
