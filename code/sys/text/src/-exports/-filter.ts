/**
 * @module
 * Text filtering and matching utilities.
 *
 * Pure helpers for matching query strings against candidate text
 * (paths, symbols, labels), with optional scoring.
 */
export * from '../m.filter/mod.ts';
